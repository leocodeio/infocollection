# Application Configuration
PORT=3001
APP_NAME=Infocollection_API
NODE_ENV=development

# URLs
# API_BASE_URL is where your API server runs (used as baseURL for better-auth)
API_BASE_URL=http://localhost:3001

# APP_BASE_URL is your frontend application URL (where users will be redirected after auth)
APP_BASE_URL=http://localhost:5173

# Swagger API Documentation
SWAGGER_ROUTE=/api-docs
SWAGGER_PASSWORD=admin

# CORS Configuration
# Must match APP_BASE_URL to allow frontend to make authenticated requests
CORS_ORIGIN='http://localhost:5173'

# Database Configuration
# PostgreSQL connection strings
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
DIRECT_URL=postgresql://user:password@localhost:5432/dbname

# Modules
## YouTube API
YOUTUBE_API_KEY=your_api_key_here

# Better Auth Configuration
# Generate a secure secret: openssl rand -base64 32
BETTER_AUTH_SECRET=sosecretsorryiwonttellyou

# Google OAuth Credentials
# Get these from: https://console.cloud.google.com/apis/credentials
# Authorized redirect URIs must include: ${API_BASE_URL}/api/auth/callback/google
BETTER_AUTH_GOOGLE_ID=your-google-client-id.apps.googleusercontent.com
BETTER_AUTH_GOOGLE_SECRET=your-google-client-secret